@model IEnumerable<Modelo.Cadastros.Departamento>

@await Html.PartialAsync("_StylesPartial")

<div class="card-block pt-3" style="width: auto">
    <div class="titulo card-header text-center">Departamentos Registrados na Instituição</div>
    <div class="card-body">
        <table>
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Instituicao.Nome)
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Nome)
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>